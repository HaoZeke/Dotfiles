extends: existence
message: "Tricolon (%s). Models default to three. Kill it: keep two items, or put the list in a table."
ignorecase: true
level: suggestion
scope: sentence
tokens:
  - '\b\w+, \w+, and \w+\b'
