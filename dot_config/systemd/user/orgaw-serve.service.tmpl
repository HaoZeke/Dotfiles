{{- if (has .chezmoi.hostname (list "rgx1gen11" "rglat5340")) -}}
[Unit]
Description=orgaw HTTP clock API (loopback)
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/orgaw serve
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
{{- end -}}
