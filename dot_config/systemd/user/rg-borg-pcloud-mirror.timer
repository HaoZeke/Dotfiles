[Unit]
Description=Daily pCloud mirror of the Terra borg repository

[Timer]
# Away from Terra's ~00:10 backup window so the mirror sees a settled repo.
OnCalendar=09:00
Persistent=true
RandomizedDelaySec=30min

[Install]
WantedBy=timers.target
