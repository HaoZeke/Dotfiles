# Managed by chezmoi. Iris Xe (Gen12) uses the iHD VA-API driver from
# intel-media-driver; pin it so libva never falls back to the legacy i965
# driver (which cannot initialise on this GPU). Applies to the systemd user
# session and the graphical apps it launches.
LIBVA_DRIVER_NAME=iHD
