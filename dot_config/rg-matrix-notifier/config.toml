user_id = "@rohit.goswami:surf.nl"
homeserver = "https://matrix.surf.nl"
body_max_chars = 160
timeout_ms = 30000
retry_seconds = 30
sync_limit = 20

[auth]
access_token_command = ["pass", "show", "matrix/rg-matrix-notifier/access-token"]

[sink]
backend = "notify-send"
urgency = "normal"
timeout_ms = 8000
open_command = ["rg-fractal-toggle", "show"]
