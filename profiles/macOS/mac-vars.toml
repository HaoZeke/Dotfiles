# Defaults
terminal = "alacritty"
 
# Machine type
machine_type = "universal-darwin"

# Program paths
gpg_path = "/run/current-system/sw/bin/gpg" # nix-shell
xauth_loc = "/opt/X11/bin/xauth" # XQuartz

################
# Conditionals #
################

is_remote = "true"
use_mamba = "true"
eval_zsh = "true"
eval_bash = "false"
