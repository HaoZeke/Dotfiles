spack:
  definitions:
    - compilers: ['gcc@10.2.0']
    - packages: [
      'zsh', 'tmux', 'git',
      'openssl', 'gmp', 'mpfr',
      'mpc', 'perl', 'cmake',
      'pcre', 'jq', 'patch',
      'ncurses', 'texinfo', 'zlib',
      # Nix
      'libseccomp', 'bdw-gc', 'brotli',
      'bison', 'flex', 'boost', 'xz',
      'curl', 'libxml2', 'autoconf',
      'bzip2', 'sqlite', 'editline'
      ]
  specs:
    - $compilers
    # - 'subversion%gcc@9.3.0'
    - matrix:
       - [$packages]
       - [$%compilers]
  view: true
