# Snippet appended guidance — we set sessiondir and cache via env; optional bind
# Install as /etc/apptainer/apptainer.conf.d is not always supported; document in profile.
# Key live settings we rely on (defaults from package already OK):
#   allow setuid = yes (rootful SIF)
#   allow user ns = yes (rootless)
#   enable overlay = yes
# Session / cache driven by APPTAINER_CACHEDIR and SINGULARITY_CACHEDIR in profile.d
