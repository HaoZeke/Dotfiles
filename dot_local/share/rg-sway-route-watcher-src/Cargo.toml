[package]
name = "rg-sway-route-watcher"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "rg-sway-route-watcher"
path = "main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
lto = true
codegen-units = 1
strip = true
