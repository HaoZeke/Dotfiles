[package]
name = "rg-space-sweep"
version = "0.2.0"
edition = "2021"

[[bin]]
name = "rg-space-sweep"
path = "main.rs"

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
