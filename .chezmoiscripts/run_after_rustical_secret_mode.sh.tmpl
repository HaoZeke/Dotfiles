#!/usr/bin/env bash
# Ensure RustiCal client secrets are not group/world readable.
set -euo pipefail
dir="${XDG_CONFIG_HOME:-$HOME/.config}/rustical"
for f in app-token.evolution frontend.password client.env; do
  p="$dir/$f"
  if [[ -f "$p" ]]; then
    chmod 600 "$p"
  fi
done
